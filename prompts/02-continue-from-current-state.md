前回の続きとして進めて下さい。

やること:
1. `PROJECT_STATUS.md` を最初に読む
2. 変更済みコードと未完了点を把握する
3. `docs/backlog.yaml` から最優先の未完了スライスを選ぶ
4. そのスライスを実装する
5. テスト・自己レビュー・状態更新まで行う

制約:
- ユーザーに過去文脈を聞き返さない
- blocker でない限り確認しない
- 仕様変更が必要なら docs を先に直してからコードへ進む

最後に:
- 完了したスライス名
- 実行結果
- 次スライス
- 残ブロッカー
