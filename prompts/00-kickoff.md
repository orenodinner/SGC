このリポジトリの実装を開始して下さい。

必須ルール:
- `AGENTS.md` を最優先で読む
- `PROJECT_STATUS.md` と `docs/` を仕様の唯一ソースとして扱う
- ユーザー確認は blocker のときだけにする
- まず repo の現状を把握し、最初の実装スライスを自分で選んで着手する
- 途中で止まらず、実装・テスト・自己レビュー・状態更新まで1セットで進める
- 口頭の計画だけで終わらない
- `PROJECT_STATUS.md` を更新する

今回の目標:
1. 開発の土台を作る
2. できれば最初の価値ある画面または最小 CRUD まで到達する
3. 次の継続がしやすいように状態を残す

優先順:
- Phase 0
- Phase 1
- Phase 2 の入口

最後に必ず出すもの:
- 何を作ったか
- 実行した確認
- 残るリスク
- 次に着手すべき最小単位
