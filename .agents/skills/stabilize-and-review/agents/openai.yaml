interface:
  display_name: "Stabilize and Review"
  short_description: "Runs focused quality stabilization and regression prevention."
  default_prompt: "Stabilize the current implementation, fix the highest-impact problems, and update status."
policy:
  allow_implicit_invocation: false
